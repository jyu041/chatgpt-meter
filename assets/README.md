# Release Assets

- `ss.png` is the current sanitized README/product screenshot.
- `logo.png` is the approved original source artwork for the extension icon.
- Generated browser icons are in `public/icon/` at 16x16, 32x32, 48x48, 96x96, 128x128, and 256x256.
- Do not describe or replace the artwork as an OpenAI or ChatGPT logo.
- Additional sanitized store screenshots may still be required before submission.

The manifest uses the generated 16x16 through 128x128 icons. The 256x256 export is retained for future listing assets.
